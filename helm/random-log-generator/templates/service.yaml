apiVersion: v1
kind: Service
metadata:
  name: {{ include "random-log-generator.fullname" . }}
  labels:
    {{- include "random-log-generator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "random-log-generator.selectorLabels" . | nindent 4 }}